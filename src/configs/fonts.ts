import { Fira_Code, Inter } from "next/font/google";

export const InterFont = Inter({ subsets: ["latin"], display: "swap" });
export const FiraCodeFont = Fira_Code({ subsets: ["latin"], display: "swap" });
